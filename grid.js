//This class creates a grid to check for close objects

class Grid  {
  
  
  
  
  constructor(texture, x, y, game) {
    super(texture, x, y, game);
 }
}



class Cell extends Grid{
  constructor(texture, x, y, game) {
    super(texture, x, y, game);
 }
}
